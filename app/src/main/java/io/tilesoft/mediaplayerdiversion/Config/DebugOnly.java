package io.tilesoft.mediaplayerdiversion.Config;

public class DebugOnly {
    /**
     * It's make an simulation debug mode
     *
     * If receive 1 (One, ON) then activated DEBUG MODE
     *
     * If receive 0 (Zero, OFF) then deactivated DEBUG MODE
     *
     * <p><b>It's is default mode it activated (ON, One)</b>.</p>
     */
    public static short DEBUG_ONLY = 1;   // ON, 1
}
